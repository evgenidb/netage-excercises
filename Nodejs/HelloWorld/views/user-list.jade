extends layout

block content
    h1= title

    // LIST USERS
    if !users || users.length < 1
        p No Users
    else
        table
            thead
                tr
                    th ID
                    th NAME
            tbody
                each user in users
                    tr
                        td= user.id
                        td= user.name


